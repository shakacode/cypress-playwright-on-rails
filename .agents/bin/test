#!/usr/bin/env bash
# Generated by shakacode/agent-workflows agent-workflow-seam-doctor --init.
set -euo pipefail
cd "$(CDPATH= cd -- "$(dirname -- "$0")/../.." && pwd)"
# The repository's supported CI gate is the default Rake task. It intentionally
# selects the portable core suite rather than every spec file in the checkout.
exec bundle exec rake
