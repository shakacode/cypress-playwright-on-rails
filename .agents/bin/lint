#!/usr/bin/env bash
# Generated by shakacode/agent-workflows agent-workflow-seam-doctor --init.
set -euo pipefail
cd "$(CDPATH= cd -- "$(dirname -- "$0")/../.." && pwd)"
# Mirrors the .github/workflows/lint.yml job: RuboCop via `rake lint`, then the
# trailing-newline check. Both go through Rake so rakelib/lint.rake stays the
# single authoritative definition of the lint gate.
exec bundle exec rake lint check_newlines
