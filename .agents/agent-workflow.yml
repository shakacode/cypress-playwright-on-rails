# Shaka's typed workflow seam.
---
version: 1
review:
  required: none
merge:
  preference: ask
branches:
  name: "{login}-{host}/{issue}-{description}"
wip:
  include_locations: false
repo_prefix: CPOR
